import { Group } from "../../../types/group.type"

type IShowGroup = {
    group: Group
}

const ShowGroup = ({group}: IShowGroup) => {
    
    return (
        <div>

        </div>
    )
}

export default ShowGroup;